# App base URL (required) — the root URL of the Context Vault app, no trailing slash
# e.g. https://app.context-vault.com
VITE_APP_BASE_URL=

# Resend (required for email capture form)
RESEND_API_KEY=
RESEND_AUDIENCE_ID=
